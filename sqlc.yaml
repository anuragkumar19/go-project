version: '2'
sql:
    - engine: 'postgresql'
      queries: './sql/queries'
      schema: './sql/schema'
      gen:
          go:
              package: 'database'
              out: 'db'
              emit_json_tags: true
